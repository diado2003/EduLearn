[auth]
# 1. PAROLA ADMIN (Existentă)
admin_password = "1234"

# 2. CLIENT SECRET (Existent)
client_secret = "YOUR_CLIENT_SECRET_VALUE"

# 3. REALM (Noul Parametru Keycloak)
# Numele realm-ului (e.g., "master" sau "my_app_realm")
realm = "YOUR_REALM_NAME"

# 4. SERVER OIDC / SERVER METADATA URL (Existent)
# Acesta este URL-ul de bază al serverului Keycloak / Google / IdP
auth_server_url = "http://localhost:8080/auth/realms/EduLearn"
# Sau păstrați ce era:
# server_metadata_url = "..."

redirect_uri = "http://localhost:8501/oauth2callback"
client_id = "xxx"